<form method="post" action="{{ path('doctor_request_delete', {'id': doctor_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doctor_request.id) }}">
    <button class="btn">Delete</button>
</form>
