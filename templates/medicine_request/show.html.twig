{% extends 'base.html.twig' %}

{% block title %}MedicineRequest{% endblock %}

{% block body %}
    <h1>MedicineRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicine_request.id }}</td>
            </tr>
            <tr>
                <th>RequestDate</th>
                <td>{{ medicine_request.RequestDate ? medicine_request.RequestDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('medicine_request_index') }}">back to list</a>

    <a href="{{ path('medicine_request_edit', {'id': medicine_request.id}) }}">edit</a>

    {{ include('medicine_request/_delete_form.html.twig') }}
{% endblock %}
